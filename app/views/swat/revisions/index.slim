= content_for :subheader do
  span
    | Revisions

#revisions-container
  .controls

  .list ng-if="revisions"
    p.revision ng-repeat="revision in revisions"
      .branch
        | {{ revision.branch }}
      .time
        | {{ revision.time }}
      .user
        | {{ revision.user }}

